<h1>Makati Disaster Information</h1>
<%= link_to 'New', new_post_path %>

<table>
  <thead>
  <td>Title</td>
  <td>Content</td>
  <td>Action</td>
  </thead>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td>
        <%= link_to 'Show', post_path(post) %>
        <%= link_to 'Edit', edit_post_path(post) %>
        <%= button_to 'Delete', post_path(post), method: :delete %>
      </td>
    </tr>
  <% end %>
</table>