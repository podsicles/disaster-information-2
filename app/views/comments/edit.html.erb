<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    
    body {
      background-color: #15202b; 
      color: #f2f2f2; 
    }

    .container {
      margin-top: 20px;
    }


    .page-header {
      font-size: 36px;
      color: white; 
      margin-bottom: 20px;
      font-weight: bold;
    }

    .edit-comment-header {
      font-size: 24px;
      color: white; 
      margin-bottom: 10px;
    }

    .form-section {
      background-color: #15202b; 
      padding: 20px;
      border-radius: 5px;
    }

    .form-label {
      color: #f2f2f2; 
    }

    .form-field {
      background-color: #1c2733; 
      color: #f2f2f2; 
      border: none; 
      border-radius: 5px;
      padding: 8px;
      width: 100%;
    }

    .btn {
      background-color: #29387f;
      color: #f2f2f2; 
      border: none; 
    }

    .btn:hover {
      background-color: white; 
      color: #29387f; 
    }
  </style>
</head>
<body>
  <h1 class="page-header ml-3 text-center">Title: <%= @post.title %></h1>

  <h2 class="edit-comment-header">Edit Comment ID: <%= @comment.id %></h2>

  <div class="container form-section">
    <%= render partial: 'form', locals: { post: @post, comment: @comment, path: post_comment_path(@post, @comment) } %>
  </div>

</body>
</html>
