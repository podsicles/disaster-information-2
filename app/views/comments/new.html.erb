<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #15202b;
      color: #f2f2f2;
    }

    .container {
      margin-top: 20px;
    }

    .form-container {
      margin: 20px;
    }

    .page-header {
      font-size: 36px;
      color: white;
      margin-bottom: 20px;
      font-weight: bold;
    }

    .form-section {
      background-color: #15202b;
      padding: 20px;
      border-radius: 5px;
    }

    .form-label {
      color: #f2f2f2;
    }

    .form-control {
      background-color: #192734;
      color: #ffffff;
      border-color: #38444d;
    }

    .form-control:focus {
      background-color: #29304d;
    }

    .btn {
      background-color: #29387f;
      color: #f2f2f2;
      border: none;
    }

    .btn:hover {
      background-color: white;
      color: #29387f;
    }
  </style>
</head>
<body>
  <h1 class="page-header ml-3 text-center">New comment Post: <%= @post.title %></h1>

  <div class="container form-section">
    <%= render partial: 'form', locals: { post: @post, comment: @comment, path: post_comments_path(@post) } %>
  </div>
</body>
</html>
