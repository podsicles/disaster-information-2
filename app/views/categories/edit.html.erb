<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #15202b; 
      color: #f2f2f2; 
    }

    h1 {
      font-size: 36px;
      color: #1da1f2; 
      font-weight: bold;
      margin-bottom: 20px;
    }

    .form-container {
      background-color: #1c2733; 
      padding: 20px;
      border-radius: 5px;
      margin-top: 20px;
    }

    .form-label {
      color: #f2f2f2; 
      font-weight: bold;
      margin-bottom: 10px;
    }

    .form-input {
      width: 100%;
      padding: 8px;
      border: 1px solid #38444d; 
      border-radius: 5px;
      background-color: #22254d; 
      color: #f2f2f2; 
      margin-bottom: 20px;
    }

    .submit-button {
      background-color: #29387f; 
      color: #f2f2f2; 
      padding: 8px 16px;
      border: none; 
      border-radius: 5px;
      font-weight: bold;
      cursor: pointer;
    }

    .submit-button:hover {
      background-color: white; 
      color: #29387f; 
    }
  </style>
</head>
<body>
  <h1 class="text-center">Category id: <%= @category.id %></h1>

  <div class="form-container">
    <%= render partial: 'form', locals: { category: @category, path: category_path(@category) } %>
  </div>

</body>
</html>
