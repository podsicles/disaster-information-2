<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #15202b;
      color: #f2f2f2;
    }

    .form-container {
      margin-top: 20px;
    }

    .form-label {
      color: #1da1f2;
    }

    .form-input {
      width: 100%;
      padding: 8px;
      border: 1px solid #38444d;
      border-radius: 4px;
      background-color: #22254d;
      color: #f2f2f2;
    }

    .form-checkbox-label {
      color: #f2f2f2;
    }

    .form-file-field {
      margin-top: 10px;
    }

    .form-submit-btn {
      background-color: #29387f;
      color: #f2f2f2;
      border: none;
      padding: 8px 16px;
      border-radius: 4px;
      cursor: pointer;
    }

    .form-submit-btn:hover {
      background-color: white;
      color: #29387f;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <%= form_with model: post do |form| %>
      <% if post.errors.any? %>
        <ul>
          <% post.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      <% end %>
      <div>
        <label class="form-label" for="post_title">Title:</label>
        <%= form.text_field :title, class: 'form-input' %>
      </div>
      <div>
        <label class="form-label" for="post_content">Content:</label>
        <%= form.text_field :content, class: 'form-input' %>
      </div>
      <div>
        <label class="form-label" for="post_address">Address:</label>
        <%= form.text_field :address, class: 'form-input' %>
      </div>
      <div>
        <label class="form-label" for="post_category_ids">Categories:</label>
        <%= form.collection_check_boxes :category_ids, Category.all, :id, :name, class: 'form-checkbox-label' %>
      </div>
      <div class="form-file-field">
        <%= form.label :image, class: 'form-label' %>
        <%= form.file_field :image %>
      </div>
      <%= form.submit 'Update', class: 'form-submit-btn' %>
    <% end %>
  </div>
</body>
</html>
