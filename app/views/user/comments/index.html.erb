<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #15202b;
      color: #f2f2f2;
    }

    .page-header {
      font-size: 36px;
      color: white;
      margin-bottom: 20px;
      font-weight: bold;
    }

    .user-info {
      color: #1da1f2;
    }

    .comment-list {
      margin-top: 20px;
      padding-left: 20px;
      list-style-type: none;
    }

    .comment-list li {
      margin-bottom: 10px;
    }

    .comment-user {
      color: #1da1f2;
      font-weight: bold;
    }

    .comment-content {
      margin-left: 10px;
    }

    .comment-link {
      color: #1da1f2;
      text-decoration: none;
    }

    .comment-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="page-header text-center">
    My Comments
  </div>

  <ul class="comment-list">
    <% @comments.each do |comment| %>
      <li>
        <strong class="comment-user"><%= comment.user.email %>: </strong>
        <span class="comment-content"><%= comment.content %></span>
        <span class="comment-link"><%= link_to 'Edit', edit_user_comment_path(comment) %></span>
        Comment from: <span class="comment-link"><%= link_to comment.post.title, post_path(comment.post) %></span>)
      </li>
    <% end %>
  </ul>

</body>
</html>
